# Mirra — https://github.com/Anna-Gura/Mirra
#
# Everything here is public documentation. The application itself sits
# behind a Google sign-in, so there is nothing private to keep out of.

User-agent: *
Allow: /

# The one place in the project that has to name the domain outright, and
# only because the format allows nothing else. Sitemaps can also be
# submitted directly in Google Search Console, which needs no file.
Sitemap: https://mirra.pages.dev/sitemap.xml
