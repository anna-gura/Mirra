/**
 * Template for js/version.js — written by build.sh at deploy time.
 *
 * Copy this file to js/version.js when working locally; the real one is
 * git-ignored and generated from the most recent git tag, so that what
 * the app reports and what was published cannot drift apart.
 */
export const VERSION = "0.0.0-dev";
